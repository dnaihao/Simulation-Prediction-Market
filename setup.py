from setuptools import setup, find_packages

setup(
    name='simulation_prediction_market',
    version='0.1',
    description='packages required for the project',
    packages=find_packages(),
    install_requires=[]
)